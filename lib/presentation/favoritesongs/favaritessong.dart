import 'package:flutter/material.dart';

class Favaritessong extends StatelessWidget {
  const Favaritessong({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Favarite song', style: TextStyle(fontSize: 50)),
      ),
    );
  }
}
